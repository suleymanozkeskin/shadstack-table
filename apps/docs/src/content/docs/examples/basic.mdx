---
title: Basic
description: Sorting, single-column filtering, and density toggle on a small dataset.
---

import { BasicTable } from '../../../components/examples/BasicTable.tsx';

A minimal table with sorting, per-column filters, and a select-filter on `role`.
The exact component code is in
`apps/docs/src/components/examples/BasicTable.tsx` if you want to copy it.

<BasicTable client:visible />
