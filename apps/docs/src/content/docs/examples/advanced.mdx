---
title: Advanced
description: Custom Cell renderers, multi-select filters, between-range filter, faceted values, row selection.
---

import { AdvancedTable } from '../../../components/examples/AdvancedTable.tsx';

Mirrors the playground's Advanced demo: a custom `Cell` for currency, a
`between` range filter (Min/Max), a `multi-select` filter for department,
faceted values, and row selection.

<AdvancedTable client:visible />
